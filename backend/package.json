{
  "name": "backend",
  "version": "1.0.0",
  "description": "The backend for mateimarica.dev",
  "main": "server.js",
  "scripts": {
    "prod": "screen cross-env NODE_ENV=production node --max-http-header-size=1024 server",
    "debug-prod": "cross-env NODE_ENV=production nodemon --max-http-header-size=1024 server",
    "dev": "cross-env NODE_ENV=development nodemon server"
  },
  "author": "Matei Marica",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-rate-limit": "^5.5.1",
    "express-subdomain": "^1.0.5",
    "helmet": "^4.6.0",
    "morgan": "^1.10.0",
    "mysql2": "^2.3.3",
    "nodemailer": "^6.7.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
